import 'package:flutter/material.dart';

class DetailMap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('detail map page')),);
  }
}